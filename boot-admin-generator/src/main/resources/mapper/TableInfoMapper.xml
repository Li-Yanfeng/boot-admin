<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boot.admin.mapper.TableInfoMapper">

    <select id="selectList" resultType="com.boot.admin.model.TableInfo">
        SELECT table_name,
               create_time,
               engine,
               table_collation,
               table_comment
        FROM information_schema.tables
        WHERE table_schema = (SELECT database())
        ORDER BY create_time DESC
    </select>

    <select id="selectPage" resultType="com.boot.admin.model.TableInfo">
        SELECT table_name,
               create_time,
               engine,
               table_collation,
               table_comment
        FROM information_schema.tables
        WHERE table_schema = (SELECT database())
        AND table_name LIKE CONCAT('%', #{tableName}, '%')
        ORDER BY create_time DESC
    </select>
</mapper>
