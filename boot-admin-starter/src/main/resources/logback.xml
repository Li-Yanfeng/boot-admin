<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="60 seconds" debug="false">

    <!--彩色日志依赖的渲染类-->
    <conversionRule conversionWord="clr" converterClass="org.springframework.boot.logging.logback.ColorConverter"/>
    <conversionRule conversionWord="wex" converterClass="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter"/>
    <conversionRule conversionWord="wEx" converterClass="org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter"/>

    <contextName>Admin</contextName>
    <property name="log.charset" value="utf-8"/>
    <property name="log.pattern" value="${CONSOLE_LOG_PATTERN:-%clr(%d{HH:mm:ss}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%10.10t]){faint} %clr(%-37.37logger{36}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}}"/>
    <property name="file.pattern" value="%d{yyyy-MM-dd HH:mm:ss} ${LOG_LEVEL_PATTERN:-%5p} --- [%10.10t] %-48.48logger{46} : %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>

    <!--日志文件保留天数-->
    <property name="maxHistory" value="30"/>
    <!--日志文件最大大小-->
    <property name="maxFileSize" value="10MB"/>

    <!--输出到控制台-->
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <charset>${log.charset}</charset>
            <pattern>${log.pattern}</pattern>
        </encoder>
    </appender>

    <!--记录到文件-->
    <appender name="file" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!--日志级别过滤-->
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>error</level>
        </filter>
        <encoder>
            <charset>${log.charset}</charset>
            <pattern>${file.pattern}</pattern>
        </encoder>
        <!--日志文件信息配置-->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxHistory>${maxHistory}</maxHistory>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>${maxFileSize}</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>
    </appender>


    <!--Spring 日志级别控制-->
    <logger name="org.springframework" level="warn" additivity="false"/>
    <!--Mybatis-Plus 日志级别控制-->
    <logger name="com.baomidou.mybatisplus" level="error" additivity="false"/>
    <!--Knife4j 日志级别控制-->
    <logger name="com.github.xiaoymin.knife4j.core.model.MarkdownFile" level="warn" additivity="false"/>
    <!--系统 日志级别控制-->
    <logger name="com.boot.admin" level="info" additivity="false"/>


    <!--dev 环境-->
    <springProfile name="dev">
        <root level="info">
            <appender-ref ref="console"/>
        </root>
    </springProfile>

    <!--prod 环境-->
    <springProfile name="prod">
        <root level="error">
            <appender-ref ref="console"/>
            <appender-ref ref="file"/>
        </root>
    </springProfile>
</configuration>
