<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boot.admin.system.mapper.MenuMapper">

    <!--通用查询映射结果-->
    <resultMap id="MenuResultMap" type="com.boot.admin.system.model.Menu">
        <id column="menu_id" property="menuId"/>
        <result column="pid" property="pid"/>
        <result column="type" property="type"/>
        <result column="title" property="title"/>
        <result column="component" property="component"/>
        <result column="component_name" property="componentName"/>
        <result column="icon" property="icon"/>
        <result column="permission" property="permission"/>
        <result column="is_i_frame" property="iFrame"/>
        <result column="path" property="path"/>
        <result column="is_cache" property="cache"/>
        <result column="is_hidden" property="hidden"/>
        <result column="menu_sort" property="menuSort"/>
        <result column="sub_count" property="subCount"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="create_by_name" property="createByName"/>
        <result column="update_by_name" property="updateByName"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!--通用查询结果列-->
    <sql id="MenuColumnList">
        menu_id, pid, type, title, component, component_name, icon, permission, is_i_frame, path, is_cache, is_hidden, menu_sort, sub_count, create_by, update_by, create_by_name, update_by_name, create_time, update_time
    </sql>

    <select id="selectMenuListByRoleId" resultMap="MenuResultMap">
        SELECT m.*
        FROM sys_role_menu rm
        INNER JOIN sys_menu m ON rm.menu_id = m.menu_id
        WHERE rm.role_id = #{roleId}
    </select>

    <select id="selectMenuListByRoleIds" resultMap="MenuResultMap">
        SELECT m.*
        FROM sys_role_menu rm
        INNER JOIN sys_menu m ON rm.menu_id=m.menu_id
        WHERE rm.role_id IN
        <foreach item='item' index='index' collection='roleIds' open='(' separator=',' close=')'>
            #{item}
        </foreach>
    </select>

    <select id="selectMenuListByRoleIdsAndType" resultMap="MenuResultMap">
        SELECT m.*
        FROM sys_menu m
        LEFT JOIN sys_role_menu rm ON m.menu_id = rm.menu_id
        LEFT JOIN sys_role r ON r.role_id = rm.role_id
        WHERE r.role_id IN
        <foreach item='item' index='index' collection='roleIds' open='(' separator=',' close=')'>
            #{item}
        </foreach>
        AND m.type &lt;&gt; #{type}
        ORDER BY m.sort ASC
    </select>
</mapper>
