<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boot.admin.system.mapper.RoleMapper">

    <!--通用查询映射结果-->
    <resultMap id="BaseResultMap" type="com.boot.admin.system.model.Role">
        <id column="role_id" property="roleId"/>
        <result column="name" property="name"/>
        <result column="level" property="level"/>
        <result column="description" property="description"/>
        <result column="data_scope" property="dataScope"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!--通用查询结果列-->
    <sql id="BaseColumnList">
        role_id, name, level, description, data_scope, create_by, update_by, create_time, update_time
    </sql>

    <select id="selectRoleListByMenuId" resultMap="BaseResultMap">
        SELECT r.*
        FROM sys_role r,
             sys_role_menu rm
        WHERE r.role_id = rm.role_id
        AND rm.menu_id IN
        <foreach item='item' index='index' collection='menuIds' open='(' separator=',' close=')'>
            #{item}
        </foreach>
    </select>

    <select id="selectRoleListByUserId" resultMap="BaseResultMap">
        SELECT r.*
        FROM sys_role r,
             sys_user_role ur
        WHERE r.role_id = ur.role_id
        AND ur.user_id = #{userId}
    </select>
</mapper>
